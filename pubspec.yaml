name: gym_management
description: 체육관 관리 데스크탑 앱

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  intl: ^0.20.2
  cached_network_image: ^3.3.1
  fl_chart: ^0.68.0
  image_picker: ^1.1.0
  file_picker: ^8.0.0
  data_table_2: ^2.5.11
  go_router: ^13.2.0
  webview_windows: ^0.4.0
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
